[workspace]
members = [
    "slopd",
    "slopctl",
    "libslop",
    "libslopctl",
    "libsloptest",
    "iroh-slopd",
    "iroh-slopctl",
]
resolver = "2"

[workspace.lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage)'] }
